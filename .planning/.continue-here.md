---
phase: directory-contract
task: 1
total_tasks: 7
status: pending
last_updated: 2026-03-04T23:20:06
---

# CONTINUE HERE - Directory Contract (Aria @architect)

## CURRENT STATE
Plano aprovado para criar Directory Contract no mega-brain usando metodologia da @architect (Aria) do aios-core.
Nenhuma task executada ainda — apenas planejamento e varredura completos.
Plano detalhado em: `.claude/plans/refactored-spinning-rivest.md`

## COMPLETED
- Varredura completa do aios-core: config.js (PATHS), analyze-project-structure.md, structure-audit skill
- Varredura completa do mega-brain: mapeados TODOS os 27 hooks, 40+ scripts, 13 CLI tools e seus outputs
- Mapa completo de "quem escreve onde" documentado no plano
- Plano aprovado pelo usuário com restrições

## RESTRICTIONS (INVIOLÁVEIS)
1. **NÃO mexer na pasta `research/`** — intocável
2. **NÃO deletar nada** — mover para `.claude/trash/` (confirmado pelo usuário)
3. **Cuidado com paths** — não quebrar nenhum script existente
4. **Enforcement total** — garantir que scripts atuais E futuros sigam o contrato

## REMAINING TASKS
1. [ ] Criar `core/paths.py` — Single Source of Truth (constantes + dict ROUTING)
2. [ ] Criar `.claude/rules/directory-contract.md` — contrato human-readable
3. [ ] Criar `.claude/hooks/directory_contract_guard.py` — hook PreToolUse (WARN not block)
4. [ ] Migrar `docs/` → `reference/` (8 úteis) + `.claude/trash/` (duplicatas)
5. [ ] Atualizar `audit_layers.py`, `validate_layers.py`, `verify_classifications.py` → importar `core/paths.py`
6. [ ] Atualizar `package.json` (21 entradas docs/→reference/) + `CLAUDE.md` (árvore) + `.gitignore`
7. [ ] Registrar hook em `settings.json` PreToolUse + verificação QA final

## DECISIONS
- @architect (Aria) do aios-core como metodologia
- `core/paths.py` = equivalente Python do config.js do aios-core
- Hook usa exit 1 (WARN) — padrão ANTHROPIC-STANDARDS
- Migração de scripts é INCREMENTAL — só 3 de audit agora, resto quando forem tocados
- Lixeira = `.claude/trash/`

## NEXT ACTION
1. Criar `core/paths.py` (código completo no plano)
2. Criar hook + contrato markdown
3. Migrar docs/ sem deletar
4. Atualizar scripts + configs

## COMMAND TO RESUME
```
Continuar implementação do Directory Contract.
Plano: .claude/plans/refactored-spinning-rivest.md
Handoff: .planning/.continue-here.md
Tasks 1-7 pendentes, começar pela Task 1 (core/paths.py).
```
